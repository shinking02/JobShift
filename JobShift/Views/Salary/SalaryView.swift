import SwiftUI

struct SalaryView: View {
    @State private var navigationTitle = "12月"

    var body: some View {
        NavigationStack {
            List {
                    
            }
            .navigationTitle(navigationTitle)
        }
    }
}
